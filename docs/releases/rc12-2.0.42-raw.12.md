# Hardened NanoKVM 2.0.42 RC12 + System 0.3.0 Raw 12

> RELEASE_BLOCKED_PENDING_OFFHOST_BACKUP_AND_RECOVERY_GATE
>
> Publication is prohibited until an encrypted production-key backup is moved
> off-host and the rebuilt full image completes its physical recovery round
> trip.

## Required install order

Existing installations cannot receive the bridge automatically because the
legacy private signing key was lost. They must first download application
**2.0.42**, verify its SHA-256, and install it through **Settings → Update →
Offline Update**. After restart, they may enable the **Preview** system channel
and install system **0.3.0-raw.12**. Format-2 system metadata enforces
`required_app_version: 2.0.42`.

Do not install the raw system payload first. The full SD-card image already
contains application 2.0.42 and does not need a separate bridge step.

## Application 2.0.42 RC12 bridge

- Includes the audited native C/C++ fixes and the current Rust backend/web UI.
- Retains signed update metadata and exact archive verification.
- Adds the format-2 app-first checks required by Raw 12.
- Installs production public key `hardened-system-prod-2026q3` alongside the
  legacy trust key and keeps all three migration key IDs allowed during RC12.
- Existing Raw 10 installations may install this app independently before
  deciding whether to try the new Buildroot/kernel system image.
- The corrected reproducible bridge archive is 22,495,269 bytes with SHA-256
  `aa0cd78c1b2e9951f216826c44b4b7ff2fb211fc1980540366f8f5b1f7e0dd20`.
  Its RISC-V server SHA-256 is
  `fd116fc459a1961cfc16fd0c7077a8f4c5e24d8f32d8745fd3b9744217083d6d`.
- The new server installs the public trust set atomically on its first start,
  which makes the transition independent of whether the already-running old
  init script can execute newly copied functions.

## System 0.3.0-raw.12

- Uses Buildroot `2026.05.1` with the accepted Raw 11 userspace security
  maintenance carried forward.
- Replaces the retained vendor kernel `5.10.4-tag-` with Linux
  `5.10.265-tag-`, based on Linux stable plus the reviewed SG2002 vendor port.
- Enables restricted `dmesg`, disables user namespaces, enables SLUB freelist
  randomisation, and enables heap initialization on allocation.
- Leaves SLUB pointer hardening and init-on-free disabled because they were not
  accepted in this release scope.
- Rebuilds all 57 runtime modules against the selected kernel and records their
  exact source provenance.

## Validation status

- Two new clean raw.12 pipelines from build commit `9567b5f` are byte-exact
  through kernel, all 57 modules, runtime staging, Buildroot ext4/tar rootfs,
  FIT, full SD image, compressed image and extracted payloads. The retained
  strict report SHA-256 is
  `d201611d70a4922cc08a1d456bd14c2c2307d35104a6ae90ac42f00283d758c2`.
- Selected A rootfs SHA-256 is
  `b840bd08f2c708b45b527d005719b6d5dc6a7b1ba66922b8e161b570a950c760`;
  the full image is
  `2b4cf8c4f9f4507a212441957a24dc33e01cf8b3283c1488a3f564f882af2ee1`;
  the 33,789,320-byte `.img.xz` is
  `a6069020dd92c024793cbb84500887a766f07263f8a6241b9462e6e0ca95dae0`.
- Exact device identity, functional runtime, the predeclared performance gate,
  ten software reboots and rollback to the accepted prior kernel pass with zero
  kernel alerts.
- During the candidate run the test device had no active HDMI input and no Wi-Fi
  hardware marker. The user explicitly waived repeating the physical
  HDMI/Wi-Fi/HID matrix; this release does not present that waiver as new
  positive hardware evidence.
- The superseded full SD image remains valid kernel/device evidence. The new
  full image containing corrected app `2.0.42` independently passes initial
  exact boot, expansion, authenticated browser/API, functional runtime, two
  post-expansion reboots and final exact/runtime/browser gates on recoverable
  media. Its evidence hashes are recorded in the final physical-gates guide.
- The corrected bridge independently reproduces and passes immediate trust,
  signature, reboot, SSH and browser-account gates from app `2.0.41`/raw.11,
  app `2.0.40`/raw.12, and the exact published app `2.0.40`/raw.10 state on
  recoverable media. The exact raw.10 result includes a physical boot and is
  not inferred from the raw.12 compatibility run.
- The installable signed raw.12 archive is 57,144,130 bytes with SHA-256
  `cf6bbef5599197f4b5ed926f22bed12352f2fdc275eb8f32fc8344d3ed7eae96`.
  Format 2 requires app `2.0.42`; metadata uses production key ID
  `hardened-system-prod-2026q3` and verifies against the production public key.
  Exact verifier-report SHA-256 is
  `d6627d560c68f57425ef6ad60b3adc47231c82bd6918330c1897e9ac0bf795b7`.
- `.133` accepted the exact signed raw bundle from app `2.0.42`/raw.11. The
  stock updater wrote rootfs before boot, restored configuration, confirmed
  boot-good, cleared the marker and disabled raw mode. Deployment report
  SHA-256 is
  `c9f0cd2ca293a1929481296d1c82b1d5a2e26fdc00d5f9a60e56db440e2c6723`.
- Initial exact runtime, an additional controlled reboot, post-reboot browser
  login, the 13-step software regression suite with five more reboots, and a
  final exact freeze all pass with zero kernel alerts. The regression report
  SHA-256 is
  `48d0a71eab8822f06efdf5dfedd379e832a5be7933f8c06a8ebffa6fda63e3e1`;
  final exact report SHA-256 is
  `a4c7eb7a509046670982719fdadd8b59ab7539da89562bb58ed59085416db49d`.

## Recovery and limitations

- The remaining recovery-round-trip step is fail-closed in
  [`../rc12-final-physical-gates.md`](../rc12-final-physical-gates.md).
- Raw 12 rewrites the boot and rootfs partitions. Keep a verified recovery SD
  image and physical card access available before starting.
- Raw partition installation has no general-purpose automatic rollback script.
  Failure recovery is by the documented physical SD reflash/recovery process.
- The full SD image is the recommended way to evaluate the new Buildroot and
  kernel combination on a spare or recoverable card.
- This release does not claim that the SG2002 vendor delta is free of every
  upstream kernel CVE; it reports the exact stable base, configuration,
  backports, provenance and tested gates instead.
- Several required vendor firmware/runtime inputs still carry `NOASSERTION`
  metadata and explicit `redistribution=disabled-pending-license-grant`
  provenance. By explicit project-owner decision this remains a prominently
  disclosed limitation of the experimental RC, but is not an RC12 publication
  blocker or a claim that the missing license metadata has been resolved.
